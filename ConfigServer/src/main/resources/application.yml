
spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Md-ObyduRahaman/Java-Microservice
          clone-on-start: true
  config:
    import: optional:configserver:http://localhost:8000

server:
  port: 8000


